spring:

  datasource:
    url: jdbc:mysql://localhost/social-login?autoReconnect=true&useSSL=false
    username: root
    password: Osbo_789
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    Show-sql: false

  thymeleaf:
    cache: false

debug: false
