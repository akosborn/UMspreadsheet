spring:

  datasource:
    url: jdbc:mysql://localhost/experimental-schema?autoReconnect=true&useSSL=false
    username: root
    password: Osbo_789
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    Show-sql: true

  thymeleaf:
    cache: false

debug: false
